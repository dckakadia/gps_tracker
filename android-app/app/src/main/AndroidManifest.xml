<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gps_tracker">

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- Device wake/battery permissions for Motorola and other manufacturers -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Motorola-specific battery/doze handling -->
    <uses-permission android:name="com.motorola.permission.MOTOROLA_MESSAGING_SERVICE" />
    <uses-permission android:name="com.motorola.permission.ACCESS_MOTOROLA_API" />
    
    <!-- Samsung-specific battery optimization bypass -->
    <uses-permission android:name="com.samsung.android.permission.INSTALL_SHORTCUT" />

    <!-- Activity Recognition — required for transition API on Android 10+ -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Legacy activity recognition for pre-Android 10 (bundled in play-services-location) -->
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <!-- AlarmManager exact alarms — required on Android 12+ for stationary wake fallback -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Push notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- In-app APK install -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="GPS Tracker"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPSTracker"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:usesCleartextTraffic,android:networkSecurityConfig">

        <service
            android:name="com.example.gps_tracker.TrackingService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <service
            android:name="com.example.gps_tracker.GpsTrackerMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.example.gps_tracker.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- System event audit: GPS disabled, airplane mode, device shutdown -->
        <receiver
            android:name="com.example.gps_tracker.SystemEventReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.location.PROVIDERS_CHANGED" />
                <action android:name="android.intent.action.AIRPLANE_MODE" />
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
            </intent-filter>
        </receiver>

        <!-- Activity Recognition transition events → wake TrackingService from STATIONARY -->
        <receiver
            android:name="com.example.gps_tracker.ActivityTransitionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.gps_tracker.ACTIVITY_TRANSITION" />
            </intent-filter>
        </receiver>

        <!-- AlarmManager fallback wake for devices without TYPE_SIGNIFICANT_MOTION sensor -->
        <receiver
            android:name="com.example.gps_tracker.StationaryWakeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.gps_tracker.STATIONARY_WAKE_CHECK" />
            </intent-filter>
        </receiver>

        <!-- GPS callback watchdog: restarts FLP when no location received for 2 min while MOVING -->
        <receiver
            android:name="com.example.gps_tracker.GpsWatchdogReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="com.example.gps_tracker.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.gps_tracker.MainActivity" android:exported="true" />

        <activity android:name="com.example.gps_tracker.DebugActivity" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>
</manifest>
