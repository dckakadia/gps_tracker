name: admin_dashboard
description: A self-hosted admin dashboard for employee location tracking.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  socket_io_client: ^2.0.0
  flutter_map: ^5.0.0
  latlong2: ^0.9.0
  provider: ^6.0.7

flutter:
  uses-material-design: true
